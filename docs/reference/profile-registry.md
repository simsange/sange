---
generated_by: tools/generators/profile_registry.py
generator_version: 1.0.0
generated_at: 2026-05-14T20:00:00Z
input_sha256: 64b1e768d1f7e8545c2fdc24d2de6a530bd68b866956aa38105a462cd82783e4
output_sha256: 29903b88429a3e76f3a4a91dd57f445abd23c7044ed5bb06b9b5af3f9e77ed53
manual_edits_allowed: false
---
# Sange gitignore profile registry

> Generated by `tools/generators/profile_registry.py` (T-G-015). Source of truth: §6.5.1 + §6.5.2 (variant composition) of `.design/sange-architecture-prompt.md`; policy in ADR-026.

Total profiles in this release: **36** (including the `_core/license` safety net). Each profile is also available as a TOML under `templates/gitignore-profiles/<category>/<name>.toml` whose integrity is covered by `templates/MANIFEST.toml.sig` (per ADR-020).

Per ADR-026: profile names are part of Sange's public API. Adding a profile is a SemVer-minor change; renaming or repurposing a profile is a SemVer-major change. Pattern revisions bump the per-profile version field independently.

## Profile catalog

| Category | Profile count |
| :--- | ---: |
| `_core/` | 3 |
| `lang/` | 11 |
| `framework/` | 9 |
| `infra/` | 5 |
| `editor/` | 5 |
| `os/` | 3 |
| **Total** | 36 |


### `_core/`

| Profile | Display name | Auto-detect signals | Extends | Patterns (always/dev/prod) | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `_core/editor-noise` | Editor + OS noise (cross-platform) | — | — | 6 / 0 / 0 | Always-on. Catches accidental commits of editor backup files. |
| `_core/license` | License + README never-exclude safety net | — | — | 8 / 0 / 0 | Reverse-pattern profile: declares paths that must NEVER be excluded from a publish tree, regardless of any other profile's ignore rules. Per §6.5.1 Red-Team Pass #5. Loaded for every repo; immutable. |
| `_core/secrets` | Secrets safety net | — | — | 13 / 0 / 0 | Always-on. Cannot be disabled without audit-logged override. |


### `lang/`

| Profile | Display name | Auto-detect signals | Extends | Patterns (always/dev/prod) | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `lang/dart` | Dart | `pubspec.yaml`, `pubspec.lock` | — | 3 / 1 / 0 | — |
| `lang/dotnet` | .NET | `*.csproj`, `*.fsproj`, `*.sln`, `global.json` | — | 6 / 0 / 0 | — |
| `lang/elixir` | Elixir | `mix.exs`, `mix.lock` | — | 3 / 3 / 0 | — |
| `lang/go` | Go | `go.mod`, `go.sum` | — | 5 / 1 / 0 | Track vendor/ when `go.mod` declares `vendor` directive; the engine warns when both signals coexist. |
| `lang/java` | Java | `pom.xml`, `build.gradle`, `build.gradle.kts`, `gradlew` | — | 5 / 3 / 0 | — |
| `lang/node` | Node.js | `package.json`, `package-lock.json`, `yarn.lock`, `pnpm-lock.yaml`, `bun.lockb` | — | 4 / 9 / 0 | — |
| `lang/php` | PHP | `composer.json`, `composer.lock` | — | 2 / 3 / 0 | — |
| `lang/python` | Python | `pyproject.toml`, `setup.py`, `requirements.txt`, `Pipfile` | — | 5 / 13 / 0 | — |
| `lang/ruby` | Ruby | `Gemfile`, `Gemfile.lock`, `*.gemspec` | — | 3 / 4 / 0 | — |
| `lang/rust` | Rust | `Cargo.toml`, `Cargo.lock` | — | 2 / 0 / 0 | Cargo.lock is tracked for binaries and gitignored in libraries; the engine doesn't make the call automatically. |
| `lang/swift` | Swift | `Package.swift`, `*.xcodeproj/`, `*.xcworkspace/` | — | 5 / 0 / 0 | — |


### `framework/`

| Profile | Display name | Auto-detect signals | Extends | Patterns (always/dev/prod) | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `framework/astro` | Astro | `astro.config.mjs`, `astro.config.ts` | `lang/node` | 2 / 0 / 0 | — |
| `framework/django` | Django | `manage.py` | `lang/python` | 3 / 3 / 0 | media/ is ignored only when MEDIA_ROOT is project-local; the operator overrides via per-project user/ profile if uploaded media is tracked intentionally. |
| `framework/flutter` | Flutter | `pubspec.yaml` | `lang/dart` | 5 / 1 / 0 | — |
| `framework/laravel` | Laravel | `artisan` | `lang/php` | 7 / 3 / 0 | — |
| `framework/nextjs` | Next.js | `next.config.js`, `next.config.mjs`, `next.config.ts` | `lang/node` | 3 / 0 / 0 | — |
| `framework/nuxt` | Nuxt | `nuxt.config.js`, `nuxt.config.ts` | `lang/node` | 3 / 0 / 0 | — |
| `framework/rails` | Ruby on Rails | `bin/rails`, `config/application.rb` | `lang/ruby` | 6 / 2 / 0 | — |
| `framework/sveltekit` | SvelteKit | `svelte.config.js`, `svelte.config.ts` | `lang/node` | 2 / 0 / 0 | — |
| `framework/symfony` | Symfony | `bin/console` | `lang/php` | 4 / 0 / 0 | — |


### `infra/`

| Profile | Display name | Auto-detect signals | Extends | Patterns (always/dev/prod) | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `infra/ansible` | Ansible | `ansible.cfg`, `inventory.yml`, `playbook.yml` | — | 4 / 0 / 0 | — |
| `infra/docker` | Docker | `Dockerfile`, `compose.yml`, `docker-compose.yml` | — | 2 / 0 / 0 | The container image itself uses a .dockerignore Sange materializes separately. |
| `infra/kubernetes` | Kubernetes | `kustomization.yaml`, `helm/Chart.yaml`, `*.k8s.yaml` | — | 2 / 0 / 0 | kubeconfig is treated as secret-class (also covered by _core/secrets). |
| `infra/pulumi` | Pulumi | `Pulumi.yaml` | — | 1 / 2 / 0 | — |
| `infra/terraform` | Terraform | `*.tf`, `*.tfvars` | — | 7 / 0 / 0 | `.terraform.lock.hcl` is dev_only-or-prod-tracked depending on team convention; default is gitignore — override via user/ profile to track. |


### `editor/`

| Profile | Display name | Auto-detect signals | Extends | Patterns (always/dev/prod) | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `editor/claude` | Claude Code | `.claude/` | — | 2 / 0 / 0 | The shared `CLAUDE.md` is tracked; only local-* overrides are gitignored. |
| `editor/emacs` | Emacs | `.emacs.d/` | — | 6 / 0 / 0 | — |
| `editor/jetbrains` | JetBrains IDEs | `.idea/` | — | 5 / 0 / 0 | — |
| `editor/vim` | Vim | `.vim/` | — | 6 / 0 / 0 | — |
| `editor/vscode` | VS Code | `.vscode/` | — | 5 / 0 / 0 | Most VSCode dot-files are user-specific; teams selectively track `settings.json` via the `!` negation pattern. |


### `os/`

| Profile | Display name | Auto-detect signals | Extends | Patterns (always/dev/prod) | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `os/linux` | Linux | — | — | 5 / 0 / 0 | Detected automatically when the host OS is Linux. |
| `os/macos` | macOS | — | — | 8 / 0 / 0 | Detected automatically when the host OS is macOS. |
| `os/windows` | Windows | — | — | 9 / 0 / 0 | Detected automatically when the host OS is Windows. |


## Auto-detection algorithm

- Walk the repo root + the first directory level for each profile's `detect.required_any`.
- Boost confidence by checking `detect.boost_any` (e.g. `uv.lock` raises confidence in `lang/python`).
- Always include `_core/secrets`, `_core/editor-noise`, and `_core/license` (safety net).
- Auto-include the appropriate `os/*` profile for the host OS.
- For each language match, include its dependent framework profiles via `extends` (Django pulls `lang/python`, Laravel pulls `lang/php`, etc.).
- For each editor whose dot-folder is present, include that editor profile.
- For infra signals, include them only when their declaring files exist.
- Emit a ranked suggestion list; the operator accepts/rejects sequentially per ADR-024.


## Composition order (when combined with the §6.5.2 Variant Matrix)

When the swap engine composes the effective `.gitignore` for the active variant, fragments merge **highest-priority first** per §6.5.2.4:

- `.sange/variants/matrix/<full-variant>/`
- `.sange/variants/stage/<stage>/`
- `.sange/variants/<dimension>/<flavor>/` — once per declared flavor dimension
- `.sange/variants/_core/`
- **This registry** (`templates/gitignore-profiles/`) — defaults


Gitignore patterns merge via **union** (a pattern ignored at any level is ignored in the composed result); use explicit `!path/pattern` negations to re-include a path a lower-level profile excluded.

## Documented-not-shipped profiles

Some languages and frameworks are covered by composition of existing profiles rather than a dedicated TOML. Listed here so the absence is intentional and discoverable:

- **Kotlin** — covered by `lang/java` + `editor/jetbrains`. Kotlin builds share `build.gradle.kts` and `settings.gradle.kts` signals with Java; shipping a separate `lang/kotlin` profile would create an auto-detect collision. Per ADR-026 v1.0 ships 35 patterns-owning profiles + the `_core/license` safety net.


## Safety profile (`_core/license`)

The `_core/license` profile is **always loaded** and rejects any composition that would exclude `LICENSE*`, `COPYING*`, `NOTICE*`, or `README*` files. Plugin-supplied profiles that violate this rule are refused at load time.

## Extending the registry

- **Add a profile** — append a `Profile(...)` literal to `PROFILES` in `tools/generators/profile_registry.py`; bump SemVer-minor on the next release.
- **Add a category** — requires an ADR (per §10.4); update the canonical category list in §10.4.1 first.
- **Plugin-supplied profile** — third-party plugins (per ADR-020 signed-manifest discipline) declare profiles in their manifest; the registry tags them `provenance: plugin (<name>)` in `sange profile list`.
- **Regenerate after a change** — `python tools/generators/all.py --only T-G-015 --write`; `verify_generated.py` enforces the integrity contract.
