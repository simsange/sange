---
generated_by: tools/generators/svn_catalog.py
generator_version: 1.0.0
generated_at: 2026-05-15T00:00:00Z
input_sha256: 852983f93d62eadd754b7647bc6f5df79af2a04ee6094127d3a97bdedb03859b
output_sha256: 66514173ad3770700b5f850d0456b2fe5f365a5a6f5231496f21ae75af6f718b
manual_edits_allowed: false
---
# Appendix E — SVN command catalog

> Generated by `tools/generators/svn_catalog.py` (T-G-002). Combines live `svn help` + `svnadmin help` with the §9.0.3 must-cover floor enrichment from `.design/sange-architecture-prompt.md`.

**SVN version used to build this catalog:** `1.14.3`. Re-run the generator after a `svn` upgrade.

**Column shape (per §9.1):** Command • Purpose • Sange wrapper • AI augmentation • Safety class • Confirmation gate • Web UI parity • Notes.

**Coverage discipline (per §9.4):** every Sange wrapper documented here must do at least one of the seven §9.4 augmentations. Pure passthrough rows are flagged in the Notes column.

## Summary

| Binary | Commands |
| :--- | ---: |
| `svn` | 35 |
| `svnadmin` | 5 |
| `svnlook` | 2 |
| `svndumpfilter` | 2 |
| `svnsync` | 2 |
| **Total** | 46 |


## `svn` (35)

| Command | Purpose | Sange wrapper | AI augmentation | Safety class | Confirmation gate | Web UI parity | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `svn add` | Put files and directories under version control. | `sange add` | AI-suggested staging groups by logical change. | Reversible | None | Yes (Commit Management) |  |
| `svn blame` | Output the content of specified files or URLs with revision and author information. | `sange blame` | Rich-rendered; AI-summarized authorship per region. | Read-only | None | Yes | Aliases: praise, annotate, ann. |
| `svn cat` | Output the content of specified files or URLs. | `sange cat` | none | Read-only | None | No |  |
| `svn changelist` | Associate (or dissociate) changelist CLNAME with the named files. | `sange changelist` | none | Reversible | None | No | Alias: cl. SVN-specific — groups working-copy files for partial commits. |
| `svn checkout` | Check out a working copy from a repository. | `sange clone <svn-url>` | AI summary of the repo on first checkout. | Read-only | None | Yes (Project & Repo Management) | Aliases: co. |
| `svn cleanup` | Recursively clean up the working copy, removing locks and resuming unfinished operations. | `sange cleanup` | none | Reversible | None | Yes (Operations) | Reads .sange/.recovery if a crash interrupted a previous SVN op. |
| `svn commit` | Send changes from your working copy to the repository. | `sange commit` / `sange commits push` | Full §6.8 commit-message lifecycle JSON; AI generation with prompt enhancer; ≥50 normalized presets. | Destructive | Y/n (per-step approval) | Yes (Commit Management) | Aliases: ci. Variant-aware (§6.5.2.6). |
| `svn copy` | Duplicate something in working copy or repository, remembering history. | `sange copy` | none | Reversible | None | No | Alias: cp. |
| `svn delete` | Remove files and directories from version control. | `sange rm` | Warns about purge for sensitive removals → §6.11. | Destructive | Y/n | No | Aliases: del, remove, rm. |
| `svn diff` | This displays the differences between two paths. | `sange diff` | Syntax-highlighted, AI-explained diff. | Read-only | None | Yes (per-commit diff view) | Alias: di. |
| `svn export` | Create an unversioned copy of a tree. | `sange export` | none | Read-only | None | No | Useful for building bundles (§6.9.4) without VCS metadata. |
| `svn import` | Commit an unversioned file or tree into the repository. | `sange import` | Pre-flight: secret scan + large-file warner. | Destructive | Y/n | No | Creates intermediate directories. One-shot only. |
| `svn info` | Display information about a local or remote item. | `sange info` | none | Read-only | None | Yes (Project view) |  |
| `svn list` | List directory entries in the repository. | `sange list` | none | Read-only | None | No | Alias: ls. |
| `svn lock` | Lock working copy paths or URLs in the repository, preventing others from committing changes. | `sange lock` | none | Reversible | None | No | SVN-specific (Git has no equivalent). |
| `svn log` | Show the log messages for a set of revision(s) and/or path(s). | `sange log` | AI-summarized 'what happened on this branch since X'. | Read-only | None | Yes (Commit Management timeline) |  |
| `svn merge` | Apply the differences between two sources to a working copy path. | `sange merge` | AI-suggested conflict resolutions; AI-generated merge-commit message. | Reversible | Y/n on conflicts | Yes (Push & Publish Approval) | Most-protected SVN verb after `commit`. |
| `svn mergeinfo` | Display merge-tracking information. | `sange mergeinfo` | none | Read-only | None | No |  |
| `svn mkdir` | Create a new directory under version control. | `sange mkdir` | none | Reversible | None | No | Creates the directory locally or remotely depending on argument. |
| `svn move` | Move (rename) an item in a working copy or repository. | `sange mv` | none | Reversible | None | No | Aliases: mv, rename, ren. |
| `svn patch` | Apply a patch to a working copy. | `sange apply` | AI-explained patch summary before apply. | Reversible | None | No | Cross-VCS — same wrapper as git apply (§9.0.2). |
| `svn propdel` | Remove a property from files, dirs, or revisions. | `sange propdel` | none | Reversible | Y/n | No | Alias: pdel, pd. |
| `svn propedit` | Edit a property with an external editor. | `sange propedit` | none | Reversible | None | No | Alias: pedit, pe. |
| `svn propget` | Print the value of a property. | `sange propget` | none | Read-only | None | No | Alias: pget, pg. |
| `svn proplist` | List all properties on files, dirs, or revisions. | `sange proplist` | none | Read-only | None | No | Alias: plist, pl. |
| `svn propset` | Set the value of a property on files, dirs, or revisions. | `sange propset` | none | Reversible | None | No | Alias: pset, ps. |
| `svn relocate` | Relocate the working copy to point to a different repository root URL. | `sange relocate` | none | Reversible | Y/n | No | Used when the repository server moves. |
| `svn resolve` | Resolve conflicts on working copy files or directories. | `sange resolve` | AI-suggested resolution strategies. | Reversible | None | Yes (Push & Publish Approval — conflicts surface) | Use `--accept theirs\|mine\|...` to specify the resolution. |
| `svn resolved` | Remove conflicted state on working copy files or directories (DEPRECATED). | `sange resolve --status resolved` | none | Reversible | None | No | Deprecated by SVN — use `svn resolve --accept working` instead. |
| `svn revert` | Restore pristine working copy state (undo local changes). | `sange revert` | AI-generated revert annotation. | Destructive | Type-to-confirm | Yes (Rollback & Recovery) | DESTRUCTIVE — destroys uncommitted local changes; no recovery. |
| `svn status` | Print the status of working copy files and directories. | `sange status` | Inline AI explanation of unusual states. | Read-only | None | Yes (Project & Repo Management) | Alias: stat, st. Variant tuple rendered above the status (§6.5.2.10). |
| `svn switch` | Update the working copy to a different URL. | `sange switch` | none | Reversible | None | Yes (Branch Management — SVN branches are URLs) | Alias: sw. |
| `svn unlock` | Unlock working copy paths or URLs. | `sange unlock` | none | Reversible | None | No |  |
| `svn update` | Bring changes from the repository into the working copy. | `sange sync` | AI-summarized incoming changes since last update. | Reversible | None | Yes (Push & Publish Approval) | Alias: up. |
| `svn upgrade` | Upgrade the metadata storage format for a working copy. | `sange upgrade` | none | Reversible | Y/n | No | One-time per WC after an SVN client upgrade. |


## `svnadmin` (5)

| Command | Purpose | Sange wrapper | AI augmentation | Safety class | Confirmation gate | Web UI parity | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `svnadmin create` | Create a new, empty repository. | `sange init --vcs svn` | Scaffolds .sange/ skeleton + auto-detects gitignore-equivalent. | Read-only | None | No | First step in any new SVN project. |
| `svnadmin dump` | Dump the contents of filesystem to stdout in a 'dumpfile' portable format. | `sange purge mirror` / `sange backup` (depending on intent) | Used by §6.11 SVN executor + §6.12 backup kit. | Reversible | None | Yes (Operations — backup) | Read-only on the repo; output goes to stdout/file. |
| `svnadmin hotcopy` | Make a hot copy of a repository (online backup). | `sange backup --hot` | none | Reversible | Y/n | Yes (Operations — backup) | The recommended SVN backup strategy; can run while repo is live. |
| `svnadmin load` | Read a 'dumpfile'-formatted stream from stdin, committing new revisions into the repository's filesystem. | `sange purge execute --vcs svn` (§6.11) | Used by the SVN purge executor in the rewrite-history pipeline. | Catastrophic | Multi-step (typed-phrase + audit waiver) | Yes (§8.2.21 Purge & History Surgery — planning only) | The destructive half of `dump→filter→load`; rewrites history. |
| `svnadmin verify` | Verify the data stored in the repository. | `sange doctor --vcs svn` | Integrated into `sange doctor` health probes. | Read-only | None | Yes (Operations — health check) |  |


## `svnlook` (2)

| Command | Purpose | Sange wrapper | AI augmentation | Safety class | Confirmation gate | Web UI parity | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `svnlook log` | Print the log message for a transaction or revision. | `sange log --raw` | Pre-commit hook reads via svnlook log. | Read-only | None | No | Used by SVN hooks (pre-commit / post-commit) for log-message inspection. |
| `svnlook tree` | Print the tree of a directory in the repository (with options to show revision info). | `sange tree` | Rich-rendered tree view per §7.0.3. | Read-only | None | No | Used by `svnadmin verify` and the §6.11 purge analyzer. |


## `svndumpfilter` (2)

| Command | Purpose | Sange wrapper | AI augmentation | Safety class | Confirmation gate | Web UI parity | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `svndumpfilter exclude` | Exclude the listed paths from the dumpfile stream. | `sange purge execute --vcs svn` (§6.11) | Used by the SVN purge executor's `dump → filter → load → swap` pipeline. | Catastrophic | Multi-step (typed-phrase + audit waiver) | Yes (§8.2.21 — planning only) | The SVN equivalent of `git filter-repo`. Wrapped by §6.11. |
| `svndumpfilter include` | Include only the listed paths in the dumpfile stream. | `sange purge execute --vcs svn --include` (§6.11) | Less-common inverse of `exclude`; same wrapper. | Catastrophic | Multi-step | Yes (§8.2.21 — planning only) |  |


## `svnsync` (2)

| Command | Purpose | Sange wrapper | AI augmentation | Safety class | Confirmation gate | Web UI parity | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `svnsync init` | Initialize a destination repository for synchronization from another repository. | `sange mirror init` | none | Reversible | None | No | Initial setup for a one-way SVN mirror. |
| `svnsync sync` | Transfer all pending revisions from the source repository to the destination. | `sange mirror sync` | none | Reversible | None | No | Idempotent; safe to run repeatedly. |


## Cross-VCS notes

- `svn merge` differs from `git merge` — SVN's merge-tracking lives in `svn:mergeinfo` properties; `sange merge --vcs svn` consults `svn mergeinfo`.
- `svndumpfilter exclude` is SVN's equivalent of `git filter-repo`; wrapped by §6.11 (`sange purge execute --vcs svn`).
- SVN has no Git-equivalent staging area — `svn commit` commits the working copy directly. The §6.8 commit-message lifecycle JSON wraps that with a review gate.
- SVN branches and tags are URL paths under `branches/` and `tags/` by convention; `sange branch` translates between SVN URL paths and the abstract Branch domain model (§7.6).
- SVN's `cleanup` recovers from a crashed operation similarly to Sange's gitignore-swap recovery (§6.5) — both leave a marker on disk and resume on the next command.


## How to update this appendix

- Add a new Sange wrapper for an SVN command → append a row to the `ENRICHMENT` tuple in `tools/generators/svn_catalog.py`.
- Upgrade SVN → the next run picks up new commands automatically; review the diff and enrich the newly-appearing rows.
- Verify integrity → `python tools/generators/verify_generated.py`.
- Regenerate → `python tools/generators/all.py --only T-G-002 --write`.
