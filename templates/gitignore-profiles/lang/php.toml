# PHP — gitignore profile.
# Generated by tools/generators/profile_registry.py (T-G-015).
# Hand-edits are discouraged; regenerate via
# `python tools/generators/all.py --only T-G-015 --write`.

[profile]
name = "lang/php"
display_name = "PHP"
category = "lang"
version = "1.0.0"
maintainer = "Simtabi LLC <opensource@simtabi.com>"
upstream_source = "https://github.com/github/gitignore/blob/main/Composer.gitignore"

[detect]
required_any = [
    "composer.json",
    "composer.lock",
]
boost_any = []

[patterns]
always = [
    "vendor/",
    "composer.phar",
]
dev_only = [
    ".phpunit.cache",
    ".phpunit.result.cache",
    ".phpunit-watcher.yml",
]
prod_only = []

[extends]
profiles = []
