# Editor + OS noise (cross-platform) — gitignore profile.
# Generated by tools/generators/profile_registry.py (T-G-015).
# Hand-edits are discouraged; regenerate via
# `python tools/generators/all.py --only T-G-015 --write`.

[profile]
name = "_core/editor-noise"
display_name = "Editor + OS noise (cross-platform)"
category = "_core"
version = "1.0.0"
maintainer = "Simtabi LLC <opensource@simtabi.com>"
notes = "Always-on. Catches accidental commits of editor backup files."

[detect]
required_any = []
boost_any = []

[patterns]
always = [
    ".DS_Store",
    "Thumbs.db",
    "desktop.ini",
    "*.swp",
    "*.swo",
    "*~",
]
dev_only = []
prod_only = []

[extends]
profiles = []
