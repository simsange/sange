# Secrets safety net — gitignore profile.
# Generated by tools/generators/profile_registry.py (T-G-015).
# Hand-edits are discouraged; regenerate via
# `python tools/generators/all.py --only T-G-015 --write`.

[profile]
name = "_core/secrets"
display_name = "Secrets safety net"
category = "_core"
version = "1.0.0"
maintainer = "Simtabi LLC <opensource@simtabi.com>"
notes = "Always-on. Cannot be disabled without audit-logged override."

[detect]
required_any = []
boost_any = []

[patterns]
always = [
    "*.pem",
    "*.key",
    "*.p12",
    "*.pfx",
    "id_rsa",
    "id_rsa.pub",
    "id_ed25519",
    "id_ed25519.pub",
    ".env",
    ".env.*",
    "!.env.example",
    "credentials*",
    "secrets*",
]
dev_only = []
prod_only = []

[extends]
profiles = []
