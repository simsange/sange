# License + README never-exclude safety net — gitignore profile.
# Generated by tools/generators/profile_registry.py (T-G-015).
# Hand-edits are discouraged; regenerate via
# `python tools/generators/all.py --only T-G-015 --write`.

[profile]
name = "_core/license"
display_name = "License + README never-exclude safety net"
category = "_core"
version = "1.0.0"
maintainer = "Simtabi LLC <opensource@simtabi.com>"
notes = "Reverse-pattern profile: declares paths that must NEVER be excluded from a publish tree, regardless of any other profile's ignore rules. Per §6.5.1 Red-Team Pass #5. Loaded for every repo; immutable."

[detect]
required_any = []
boost_any = []

[patterns]
always = [
    "!LICENSE",
    "!LICENSE.*",
    "!COPYING",
    "!COPYING.*",
    "!NOTICE",
    "!NOTICE.*",
    "!README",
    "!README.*",
]
dev_only = []
prod_only = []

[extends]
profiles = []
