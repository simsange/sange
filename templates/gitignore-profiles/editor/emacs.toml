# Emacs — gitignore profile.
# Generated by tools/generators/profile_registry.py (T-G-015).
# Hand-edits are discouraged; regenerate via
# `python tools/generators/all.py --only T-G-015 --write`.

[profile]
name = "editor/emacs"
display_name = "Emacs"
category = "editor"
version = "1.0.0"
maintainer = "Simtabi LLC <opensource@simtabi.com>"

[detect]
required_any = [
    ".emacs.d/",
]
boost_any = []

[patterns]
always = [
    "*~",
    "\\#*\\#",
    ".\\#*",
    "auto-save-list/",
    "tramp",
    "*_archive",
]
dev_only = []
prod_only = []

[extends]
profiles = []
