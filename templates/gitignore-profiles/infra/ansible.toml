# Ansible — gitignore profile.
# Generated by tools/generators/profile_registry.py (T-G-015).
# Hand-edits are discouraged; regenerate via
# `python tools/generators/all.py --only T-G-015 --write`.

[profile]
name = "infra/ansible"
display_name = "Ansible"
category = "infra"
version = "1.0.0"
maintainer = "Simtabi LLC <opensource@simtabi.com>"

[detect]
required_any = [
    "ansible.cfg",
    "inventory.yml",
    "playbook.yml",
]
boost_any = []

[patterns]
always = [
    "*.retry",
    "roles/*.tar.gz",
    "ansible.log",
    "*.vault",
]
dev_only = []
prod_only = []

[extends]
profiles = []
