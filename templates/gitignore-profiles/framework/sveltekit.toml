# SvelteKit — gitignore profile.
# Generated by tools/generators/profile_registry.py (T-G-015).
# Hand-edits are discouraged; regenerate via
# `python tools/generators/all.py --only T-G-015 --write`.

[profile]
name = "framework/sveltekit"
display_name = "SvelteKit"
category = "framework"
version = "1.0.0"
maintainer = "Simtabi LLC <opensource@simtabi.com>"

[detect]
required_any = [
    "svelte.config.js",
    "svelte.config.ts",
]
boost_any = []

[patterns]
always = [
    ".svelte-kit/",
    "build/",
]
dev_only = []
prod_only = []

[extends]
profiles = [
    "lang/node",
]
